<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <script src="https://cdn.tailwindcss.com"></script>
    <title>Edit</title>
</head>
<body>
    <div class="w-full min-h-screen bg-zinc-900 text-white p-10">
        <div class="w-full flex justify-end">
            <a href="/logout" class="bg-red-500 px-3 py-2 rounded-md text-sm mb-5 inline-block">Logout</a>
        </div>
        <h5 class="font-light mb-5">Edit you post</h5>
        <form action="/update/<%= post._id %>" method="post">
            <textarea class="block w-1/3 resize-none outline-none bg-transparent border-2 border-zinc-800 rounded-md p-3" placeholder="what's on your mind ?"  name="content" id=""><%= post.content %></textarea>
            <input type="submit" class="text-sm px-3 py-3 bg-yellow-500 block text-black rounded-md mt-2 w-40" value="Update Post">
        </form>

    </div>
</body>
</html>     