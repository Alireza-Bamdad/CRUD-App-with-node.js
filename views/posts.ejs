<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <script src="https://cdn.tailwindcss.com"></script>
    <title>Posts</title>
</head>
<body>
    <div class="w-full min-h-screen bg-zinc-900 text-white p-10">
        <div class="w-full flex justify-between">
            <a href="/logout" class="bg-red-500 px-3 py-2 rounded-md text-sm mb-5 inline-block">Logout</a>
            <a href="/profile" class="bg-blue-500 px-3 py-2 rounded-md text-sm mb-5 inline-block">profile</a>

        </div>
        <h3 class="text-3xl mb-1 font-light  text-center">You Can See All Posts 👀</h3>
        <div class="flex items-start justify-around ">
            <div class="posts mt-20 text-zinc-400 w-96 ">
                <div class="postcointainer  mt-4 w-full flex flex-col  ">
                    <% posts.reverse().forEach(function(post){%>
                        <div class="post w-1/3 p-4  w-full rounded-md border-[1px] mb-4 border-zinc-800 bg-zinc-800">
                            <h4 class="text-blue-500 mb-2">@<%=post.username%></h4>
                            <p class="text-sm tracking-thight"><%= post.content %></p>
                            <small class="mt-2 inline-block"><%= post.likes.length %> likes</small>
                            <div class="btns flex mt-1 gap-4">
                                <a href="/like/<%=post._id%>" class="text-blue-500">
                                    <%= post.likes.indexOf(user._id) === -1 ?  "Like" : "Unlike " %>
                                </a>
                            </div>
                        </div> 
                    <% }) %>
                </div>
            </div>
            <img src="/images/3d-casual-life-young-boys-and-young-girl-jumping.png" class="mt-20 mr-10" alt="">
        </div>

    </div>
</body>
</html>     