<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <script src="https://cdn.tailwindcss.com"></script>
    <title>Dashbord</title>
</head>
<body>
    <div class="w-full min-h-screen bg-zinc-900 text-white p-10">
        <div class="w-full flex justify-between">
            <a href="/logout" class="bg-red-500 px-3 py-2 rounded-md text-sm mb-5 inline-block">Logout</a>
            <a href="/posts" class="bg-blue-500 px-3 py-2 rounded-md text-sm mb-5 inline-block">Posts</a>

        </div>
        <div class="flex mt-20">
            <div class="w-full">
                <h3 class="text-3xl mb-1"><span class="font-light">Hello</span>, <%=user.name%>👋</h3>
                <h5 class="font-light mb-5">you can create a new post</h5>
                <form action="/post" method="post">
                    <textarea class="block w-1/3 resize-none outline-none bg-transparent border-2 border-zinc-800 rounded-md p-3" placeholder="what's on your mind ?"  name="content" id=""></textarea>
                    <input type="submit" class="text-sm px-3 py-3 bg-blue-500 block rounded-md mt-2 w-40" value="Create New Post">
                </form>
            </div>
            <img src="/images/3d-casual-life-woman-talking-with-chatbot.png" alt="">
        </div>
        <div class="posts mt-10 text-zinc-400">
            <h3>Your Posts.</h3>
            <div class="postcointainer mt-4">
                <% user.posts.reverse().forEach(function(post){%>
                    <div class="post w-1/3 p-4 rounded-md border-[1px] mb-4 border-zinc-800 bg-zinc-800">
                        <h4 class="text-blue-500 mb-2">@<%=user.username%></h4>
                        <p class="text-sm tracking-thight"><%= post.content %></p>
                        <small class="mt-2 inline-block"><%= post.likes.length %> likes</small>
                        <div class="btns flex mt-1 gap-4">
                            <a href="/edit/<%= post._id %>" class="text-zinc-600">Edit</a>
                            <a href="/delete/<%=post._id%>" class="text-red-500">Delete</a>
                        </div>
                    </div> 
                <% }) %>
            </div>
        </div>
    </div>
</body>
</html>     